# 记忆宫殿 v8.0.6：插件内软遗忘与确认删除

本说明描述当前实现。自动流程只生成待删除候选，不会直接删除 Roche 主记忆。

## 评分模型

插件使用艾宾浩斯形式的保持率：

```text
R = e^(-t/S)
```

其中 `t` 是距离上次插件内复习的天数，`S` 会受到以下因素影响：

- 重要性越高，基础强度越高。
- 复习次数越多，强度越高。
- 卧室、书房等房间有不同的衰减率。
- 低保持率会进入软遗忘状态，但不会被过滤出 Roche。

状态阈值：

| 状态 | 保持率 | 含义 |
| --- | ---: | --- |
| `active` | >= 30% | 当前保持较好 |
| `fading` | < 30% | 低保持率，需要时可在面板复习 |
| `faded` | < 10% | 已淡忘标记，等待用户判断 |

删除候选还必须满足插件综合评分 <= 25。达到条件后记忆进入待确认列表。

## 客厅水位线

当插件看到客厅超过 200 条记忆时：

1. 按 `importance × retention` 从低到高排序。
2. 只处理超出 200 条的部分。
3. 重要性 >= 7 的记忆迁移到插件内的卧室元数据。
4. 其余记忆迁移到插件内的阁楼元数据。
5. 保存 `softForgetState`、房间和评分到 `memoryMeta:{conversationId}`。

迁移只发生在插件自己的对象和 storage 中。Roche 的事实记忆、向量记忆、原文内容和原生召回顺序都不改变。

## 用户复习

在面板中点击记忆卡片会：

- 更新插件自己的 `lastRecall`。
- 增加插件自己的 `reviewCount`。
- 重新计算保持率和软遗忘状态。

不会调用 `memory.write` 或 `memory.update`。只有用户在遗忘中心二次确认后，才会调用 `memory.delete`。

## 删除策略

当前版本不会自动删除 Roche 主记忆。保持率 <= 10% 且插件评分 <= 25 的记忆会进入待确认列表。用户点击“确认删除”后还会看到不可逆提示，确认后才调用 `roche.memory.delete(id)`；点击“保留”则会退出待删除列表。

## 遗忘中心

主页的“遗忘中心”会把所有记忆按插件评分从低到高列出，并提供 `fading`、`faded` 和“待确认删除”筛选。用户可以手动确认插件内的软遗忘、复习恢复，或对待删除项目进行二次确认。删除调用 Roche API，但不会改写其他记忆的向量分数。

## 日志

客厅触发迁移时，控制台会记录类似：

```text
[记忆宫殿] 自动遗忘：50 条记忆迁移（卧室 8 / 阁楼 42）
```

这条日志表示插件元数据的房间迁移，不表示 Roche 主记忆被自动删除或更新。

当前版本：`8.0.6`
