# 记忆宫殿 v8.0.4 使用指南

## 这版插件做什么

记忆宫殿是一个独立的只读分析面板：它读取 Roche 原有记忆，按本地遗忘曲线计算保持率，并在自己的 storage 元数据中记录分类、重要性、情绪、复习次数和软遗忘状态。

Roche 原有自动记忆、主记忆内容、向量记忆分数和聊天召回都保持原样。插件没有 AI 对话扩展，不会改变 AI 看到的上下文。

## 安装或更新

在 Roche 插件管理中安装或刷新以下 manifest：

```text
https://raw.githubusercontent.com/zxinyi404-maker/roche-memory-palace/main/manifest.json
```

安装后确认版本为 `8.0.4`。如果 Roche 使用了旧缓存，可先卸载旧版本，再重新安装 manifest。

## 面板操作

1. 选择一个 Roche 会话。
2. 进入记忆宫殿查看七个房间的分布。
3. 使用“查看全部记忆”、事件盒或搜索页面浏览记忆。
4. 打开“遗忘曲线分析”查看平均保持率和低保持率数量。
5. 点击记忆卡片表示一次复习，插件只更新自己的复习次数和保持率。

## 软遗忘规则

保持率由时间、重要性、情绪、复习次数和所在房间共同决定：

```text
R = e^(-t/S)
```

- 保持率低于 30%：标记为低保持率（软遗忘），仍保留在 Roche 中。
- 保持率低于 10%：标记为已淡忘，仍保留在 Roche 中。
- 客厅超过 200 条：按“重要性 × 保持率”排序，把超出的记忆在插件内迁移到卧室或阁楼。
- 重要性不低于 7 的超额记忆进入卧室，其余进入阁楼。

这些动作只写入 `memoryMeta:{conversationId}`，不会调用 Roche 的记忆写入、更新或删除 API。

## 如何手动清理

当前版本不会自动删除 Roche 主记忆，也没有批量删除按钮。确认某条记忆确实不需要后，请使用 Roche 原生记忆管理界面手动删除。插件的软遗忘状态不会替代这一步确认。

## 数据和权限

插件读取：会话、角色、人设和 Roche 长短期记忆。

插件写入：仅 `roche.storage` 中的 `memoryMeta:{conversationId}` 元数据。

插件不使用：`memory.write`、`memory.update`、`memory.delete`、`ai:chat` 或 `chat.contextProvider`。

## 常见问题

### 为什么插件的排序没有改变 Roche 的召回？

这是设计目标。插件中的本地相关度只用于面板显示和分析，不会注入聊天，也不会修改 Roche 的向量分数。

### 复习后 Roche 主记忆会被更新吗？

不会。复习只更新插件 storage 中的 `lastRecall`、`reviewCount` 和软遗忘状态。

### 卸载插件会删除 Roche 记忆吗？

不会。卸载只会让插件自己的元数据不再被读取，Roche 主记忆仍由 Roche 管理。

## 本地检查

```bash
node --check plugin.js
```

当前版本：`8.0.4`
