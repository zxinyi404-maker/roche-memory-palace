# AI 对话集成说明（已停用）

旧版 v8.0 曾通过 `chat.contextProvider` 把插件筛选出的记忆注入 Roche 主对话。为保证 Roche 原生向量记忆召回不受影响，当前 `8.0.6` 已移除这项集成。

当前插件：

- 没有 `chat` 配置或 `contextProvider`。
- 不调用 `ai:chat`。
- 不读取短期消息来构造聊天提示。
- 不改变 Roche 的自动记忆、向量分数或对话上下文。
- 只在插件面板中展示本地保持率、相关度排序和软遗忘状态。

旧版文档中的“同步到 Roche”“AI 能感知记忆宫殿权重”等步骤不适用于当前版本，请以 `README.md` 和 `使用指南_v8.0.md` 为准。

当前版本：`8.0.6`
