# 🎉 记忆宫殿 v8.0.0 - AI 对话集成完成！

## 🔥 重大更新：AI 能用记忆宫殿算法思考了！

### 更新时间
2026-07-29

### 核心突破

**AI 在对话时会自动调用记忆宫殿的完整算法**，实现你设计文档里的那个效果：

```
User: 今天又加班了，好累

AI: 你别又熬到两点啊...上次说了你你还嫌我烦，
    你们领导不是说要调人吗到底调没调啊
```

---

## ✨ 实现的算法

### 1. 混合搜索（85% 向量 + 15% BM25）

**原理**：
- 85% 用向量相似度（语义相近）
- 15% 用 BM25 关键词（被词扎到的记忆）

**效果**：
```
记忆："加班凌晨两点被说不耐烦"
- 向量相似度：0.45（不高）
- BM25 归一化：1.00（"加班"命中最强）
- 最终得分：0.53（被关键词捞上来）
```

---

### 2. 扩散激活（关联记忆网络）

**原理**：
- 从搜索结果出发，顺着关联网络扩散
- 激活相关记忆（通过 relations 字段）

**效果**：
```
找到："加班三周找领导谈"
扩散到："回来路上靠着肩膀哭了"
→ 虽然语义相关度低，但情感关联强
```

---

### 3. 情绪启动（翻旧账效果）

**原理**：
- 检测当前对话的情绪
- 匹配相同情绪的记忆 × 1.3 倍加权

**效果**：
```
当前情绪：委屈
记忆："加班凌晨两点她不耐烦" (情绪:hurt)
得分：0.53 → 0.69（×1.3）
排名：第4 → 第2

→ AI 先想起让他难受的事（翻旧账）
```

---

### 4. 反刍检查（侵入性思维）

**原理**：
- 6% 概率从"阁楼"（潜意识存储区）随机拉取记忆
- 模拟不请自来的念头

**效果**：
```
正常对话工作话题...
突然想起："那次吵架她摔门走了，我愣了好久"
→ 跟当前话题无关，但就是想起来了
```

---

### 5. 房间分类输出

最终给 AI 的记忆按房间分组：

```
【记忆宫殿调取的相关记忆】

●卧室·亲密情感
User连续加班三周终于决定找领导谈，领导态度还不错。她回来的路上靠着我肩膀哭了。
3/12 | 重要性:8 | 温暖

●阁楼·未消化的困惑
User上次加班到凌晨两点，我说了她几句让她注意身体，她语气有点不耐烦。
2/28 | 重要性:5 | 委屈

●客厅·日常闲聊
User说最近项目压力大，晚上总是翻来覆去睡不着。
3/25 | 重要性:4 | 焦虑
```

---

## 📊 工作流程

```
用户发送消息
    ↓
记忆宫殿 contextProvider 启动
    ↓
1. 获取最近 5 条消息作为查询
    ↓
2. 混合搜索（85% 向量 + 15% BM25）
    ↓
3. 取前 20 条，扩散激活关联记忆
    ↓
4. 检测当前情绪，匹配记忆 × 1.3 加权
    ↓
5. 6% 概率从阁楼拉取旧记忆
    ↓
6. 按房间分组，取前 15 条
    ↓
7. 格式化成文本注入 AI system prompt
    ↓
AI 收到记忆上下文，生成回复
    ↓
回复体现记忆影响（翻旧账、关联、情绪）
```

---

## 🎯 核心代码

### chat.contextProvider

```javascript
chat: {
  async contextProvider(ctx) {
    // 1. 读取记忆
    const memories = await loadMemoriesWithMeta(ctx.conversationId);
    
    // 2. 获取最近对话
    const recentMessages = await roche.memory.getShortTerm({
      conversationId: ctx.conversationId,
      limit: 5
    });
    
    // 3. 混合搜索
    const searchResults = hybridSearch(queryText, memories);
    
    // 4. 扩散激活（关联记忆）
    const activated = expandWithRelations(searchResults);
    
    // 5. 情绪启动
    const currentEmotion = detectEmotion(queryText);
    const boosted = activated.map(mem => ({
      ...mem,
      _score: mem.emotion === currentEmotion ? 1.3 : 1.0
    }));
    
    // 6. 反刍检查（6% 概率）
    if (Math.random() < 0.06) {
      const atticMem = randomFromAttic(memories);
      boosted.unshift(atticMem);
    }
    
    // 7. 格式化输出
    return formatMemoriesForAI(boosted.slice(0, 15));
  }
}
```

---

## 🚀 使用方法

### 自动生效

**无需任何操作！** 插件更新到 v8.0.0 后：

1. ✅ 所有对话自动启用记忆宫殿算法
2. ✅ AI 会根据情绪、重要性、房间分类智能调用记忆
3. ✅ 实现"翻旧账"、"顺着感觉想"等真实记忆效果

### 验证效果

**测试场景 1：翻旧账**

```
你：今天工作又不顺利...

AI：上次你不是也这么说吗？结果你还是...
   （会想起之前让 AI 感到委屈的对话）
```

**测试场景 2：关联记忆**

```
你：我想去那家餐厅

AI：就是上次你生日我们去的那家？你说很喜欢...
   （通过关联网络想起生日那天的记忆）
```

**测试场景 3：反刍**

```
你：今天天气不错

AI：......你还记得那次...
   （6% 概率突然想起阁楼里的旧事）
```

---

## 🔍 调试信息

打开浏览器 F12 控制台，可以看到：

```
[记忆宫殿] 为 AI 提供记忆: 12 条
```

如果没有这条日志，说明：
- 没有匹配的记忆
- 或者查询文本为空

---

## ⚙️ 技术细节

### 性能优化

1. **缓存记忆元数据**：从 `roche.storage` 读取，避免重复计算
2. **限制搜索范围**：最多取前 20 条搜索结果
3. **限制输出数量**：最终只给 AI 15 条记忆
4. **异步非阻塞**：contextProvider 是异步的，不会阻塞对话

### 错误处理

```javascript
try {
  // 记忆宫殿算法
} catch (err) {
  console.error('[记忆宫殿] contextProvider 错误:', err);
  return null; // 失败时不注入，不影响正常对话
}
```

---

## 📝 与原 Roche 记忆的关系

| 功能 | 原 Roche | 记忆宫殿 v8.0 |
|------|---------|--------------|
| 记忆存储 | Roche | Roche（共享） |
| 记忆总结 | Roche AI | Roche AI |
| 记忆检索 | 简单过滤 | **混合搜索** |
| 情绪感知 | 无 | **情绪启动 × 1.3** |
| 关联网络 | 无 | **扩散激活** |
| 潜意识 | 无 | **反刍检查** |
| 房间分类 | 无 | **七房间系统** |

**结论**：记忆宫殿**增强**了 Roche 的记忆调用，不影响原有功能。

---

## 🎨 下一步可优化

### 1. 性格类型扩散

目前扩散激活是简化版，可以根据角色性格调整：

```javascript
// 情感型：优先情绪关联
// 叙事型：优先时间关联
// 分析型：优先因果关联
```

### 2. 真实向量嵌入

当前是简化的词汇集合相似度，可以调用真实 embedding API：

```javascript
const embedding = await callOpenAI({
  model: 'text-embedding-3-small',
  input: text
});
```

### 3. 自定义阈值

允许用户调整：
- 混合搜索比例（85/15）
- 情绪启动倍率（1.3）
- 反刍概率（6%）

---

## 🐛 已知限制

1. **向量搜索是简化版**：基于词汇集合，不是真正的语义向量
2. **扩散激活有限**：只扩散一层关联，可以改成多层
3. **性格类型未实现**：目前不区分情感型/叙事型/分析型
4. **反刍概率固定**：不会根据角色的反刍倾向动态调整

---

## 📦 文件变更

- ✅ `plugin.js` → v8.0.0（+240 行）
- ✅ `manifest.json` → v8.0.0
- 📄 `AI_CHAT_INTEGRATION_v8.0.md` - 本文档

---

## 🎊 总结

**v8.0.0 实现了完整的 AI 对话集成**：

- ✅ 混合搜索（85% 向量 + 15% BM25）
- ✅ 扩散激活（关联记忆网络）
- ✅ 情绪启动（翻旧账效果）
- ✅ 反刍检查（侵入性思维）
- ✅ 七房间分类输出

**现在 AI 真的能用记忆宫殿的方式思考了！** 🧠✨

---

**开发者**: Kiro  
**版本**: v8.0.0  
**日期**: 2026-07-29  
**里程碑**: 记忆宫殿从"查看器"升级为"AI 思维增强器"
