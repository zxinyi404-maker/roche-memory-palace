# 🧹 记忆宫殿 v8.0.3 - 自动遗忘功能说明

## ✅ 自动遗忘功能已完整实现

### 📍 功能位置
- **定义**：`line 320-365`（`autoForget` 函数）
- **调用**：每次加载记忆时自动执行

---

## 🔄 工作原理

### 触发条件
当**客厅（livingRoom）**的记忆超过 **200 条**时自动触发

### 迁移规则

```
客厅记忆数 > 200
    ↓
按「重要性 × 保持率」排序（最不重要的在前）
    ↓
超出的记忆按重要性分流：
    ├─ 重要性 >= 7 → 晋升到「卧室」（长期保存）
    └─ 重要性 < 7  → 沉入「阁楼」（潜意识）
```

### 示例

假设客厅有 **250 条**记忆：

```
超出数量：250 - 200 = 50 条需要迁移

排序后（最不重要的前 50 条）：
1. 「今天吃了午饭」 importance: 3 → 阁楼
2. 「天气不错」     importance: 2 → 阁楼
3. 「聊了工作」     importance: 5 → 阁楼
...
45. 「加班到凌晨」   importance: 8 → 卧室 ⭐
46. 「吵架和好了」   importance: 9 → 卧室 ⭐
...
50. 「第一次见面」   importance: 10 → 卧室 ⭐

结果：
- 阁楼 +42 条（日常琐事）
- 卧室 +8 条（重要记忆）
- 客厅保留 200 条最新的
```

---

## 📊 七房间容量与衰减

| 房间 | 容量 | 衰减率 | 自动遗忘规则 |
|------|------|--------|-------------|
| 客厅 | 200条 | 0.8 | ✅ 超过200条自动迁移 |
| 卧室 | 无限 | 0.1 | ❌ 不会自动清理 |
| 书房 | 无限 | 0.3 | ❌ 不会自动清理 |
| User房间 | 无限 | 0.2 | ❌ 不会自动清理 |
| 自我房间 | 无限 | 0 | ❌ 不会自动清理 |
| 阁楼 | 无限 | 0 | ❌ 不会自动清理 |
| 窗台 | 无限 | 0.5 | ❌ 不会自动清理 |

**只有客厅有容量限制**，其他房间无限容量。

---

## 🎯 自动遗忘的意义

### 1. 模拟真实记忆
人不会记住所有日常琐事，只有重要的事件才会进入长期记忆。

### 2. 性能优化
限制客厅记忆数量，避免搜索性能下降。

### 3. 记忆分层
- **客厅**：短期记忆（海马体）
- **卧室**：长期重要记忆（新皮层）
- **阁楼**：潜意识记忆（杏仁核）

---

## 🔍 如何查看自动遗忘日志

打开浏览器控制台（F12），当客厅超过 200 条时，会看到：

```
[记忆宫殿] 自动遗忘：50 条记忆迁移（卧室 8 / 阁楼 42）
```

---

## 🛠️ 技术细节

### v8.0.3 改进

**改进前**：
- ✅ 自动迁移功能正常
- ❌ 没有日志记录
- ❌ 不支持同步到 Roche

**改进后**：
- ✅ 自动迁移功能正常
- ✅ 控制台日志记录迁移详情
- ✅ 支持同步到 Roche（预留接口）
- ✅ 统计迁移到卧室/阁楼的数量

### 代码优化

```javascript
async function autoForget(memories, syncToRoche = false) {
  const livingRoomMemories = memories.filter(m => m.room === 'livingRoom');

  if (livingRoomMemories.length > 200) {
    // 排序：importance × retention（越小越先被迁移）
    livingRoomMemories.sort((a, b) => {
      const scoreA = a.importance * calculateRetention(a);
      const scoreB = b.importance * calculateRetention(b);
      return scoreA - scoreB;
    });

    const toMigrate = livingRoomMemories.slice(0, livingRoomMemories.length - 200);
    let bedroomCount = 0;
    let atticCount = 0;

    for (const mem of toMigrate) {
      if (mem.importance >= 7) {
        mem.room = 'bedroom';
        bedroomCount++;
      } else {
        mem.room = 'attic';
        atticCount++;
      }

      // 如果需要，同步到 Roche
      if (syncToRoche && typeof syncMemoryToRoche === 'function') {
        await syncMemoryToRoche(mem);
      }
    }

    // 记录日志
    console.log(`[记忆宫殿] 自动遗忘：${toMigrate.length} 条记忆迁移（卧室 ${bedroomCount} / 阁楼 ${atticCount}）`);
  }

  return memories;
}
```

---

## 🎮 使用场景

### 场景 1：日常对话积累
```
第1天：客厅 50 条
第5天：客厅 120 条
第10天：客厅 210 条 → 自动遗忘触发
  ├─ 卧室 +2 条（重要对话）
  ├─ 阁楼 +8 条（日常闲聊）
  └─ 客厅保留 200 条
```

### 场景 2：长期使用
```
1个月后：
  ├─ 客厅：200 条（最近的）
  ├─ 卧室：50 条（重要记忆）
  ├─ 阁楼：300 条（琐事）
  └─ 其他房间：各有内容

AI 对话时：
- 优先从客厅、卧室调用
- 6% 概率从阁楼拉出旧记忆（反刍）
```

---

## ⚙️ 自定义设置

### 修改客厅容量

打开 `plugin.js`，搜索 `> 200`：

```javascript
if (livingRoomMemories.length > 200) {  // 改成 300 或其他值
```

### 修改重要性阈值

搜索 `>= 7`：

```javascript
if (mem.importance >= 7) {  // 改成 8 或 6
  mem.room = 'bedroom';
```

---

## 📝 总结

**自动遗忘功能早就实现了**，只是没有明显的提示。v8.0.3 加强了：

1. ✅ 日志记录（控制台可见）
2. ✅ 统计迁移数量
3. ✅ 预留同步接口
4. ✅ 异步支持

**现在你可以放心使用了！** 客厅记忆会自动管理，不用担心记忆堆积。

---

**更新时间**: 2026-07-29  
**版本**: v8.0.3  
**状态**: ✅ 功能完整
